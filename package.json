{
  "name": "kn360-command-center",
  "version": "1.0.0",
  "description": "Kulwant's 360° marketing command center: one dashboard for LinkedIn, Instagram, Facebook, X, Threads and the newsletter.",
  "private": true,
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check lib/store.js && node --check lib/auth.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
