services:
  dashboard:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      DATA_DIR: /app/data
    volumes:
      - dashboard-data:/app/data

volumes:
  dashboard-data:
